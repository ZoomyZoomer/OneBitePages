{
    "buildCommand": "cd blogsite && yarn install && ./node_modules/vite/bin/vite.js build",
    "outputDirectory": "blogsite/dist",
    "framework": "vite",
    "rewrites": [
      {
        "source": "/api/(.*)",
        "destination": "/api/index.js"
      },
      {"source": "/(.*)", "destination": "/"}
    ]
  }