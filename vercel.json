{
    "buildCommand": "cd blogsite && npm install && ./node_modules/vite/bin/vite.js build",
    "outputDirectory": "blogsite/dist",
    "framework": "vite",
    "functions": {
      "api/post": {
        "maxDuration": 10
      }
    },
    "rewrites": [
      {
        "source": "/api/(.*)",
        "destination": "/api/index.js"
      }
    ]
  }